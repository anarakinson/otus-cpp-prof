name: 'C++ bayan'


on:
  push:
    branches:
      - main
      - feature/github_actions
    paths:
      - '08.bayan/**'
  workflow_dispatch:



jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      ### Install requirements ###
      - name: Install requirements
        working-directory: ./08.bayan/ 
        run: |
             sudo apt-get update && sudo apt-get install cmake boost -y 
      ### Build ###
      - name: Build 08.bayan
        working-directory: ./08.bayan/
        run: |
             cmake -B build -DPATCH_VERSION=${{ github.run_number }}
             cmake --build build
             cmake --build build --target package
             ls -la .
             ls -la build
             
             
      
      ######################
      ### Create release ###
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: 10${{ github.run_number }}
          release_name: Release - 08.bayan - ${{ github.run_number }}
          draft: false
          prerelease: false
        
      ### Upload release ###
      - name: Upload "08.bayan" Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./08.bayan/build/bayan-0.0.${{ github.run_number }}-Linux.deb
          asset_name: bayan-0.0.${{ github.run_number }}-Linux.deb
          asset_content_type: application/vnd.debian.binary-package

